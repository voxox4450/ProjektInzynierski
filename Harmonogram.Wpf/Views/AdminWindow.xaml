<hc:Window x:Class="Harmonogram.Wpf.Views.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
        xmlns:local="clr-namespace:Harmonogram.Wpf.Views" 
        xmlns:viewmodels="clr-namespace:Harmonogram.Wpf.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:AdminViewModel}"
           md:DialogServiceViews.IsRegistered="True"
           mc:Ignorable="d"
        Title="AdminWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>


        </Grid.RowDefinitions>
        <!--AdminMenu-->
        <Menu Grid.Row="0" x:Name="menuAdminPanel" FontSize="20" FontWeight="Light" Background="Transparent">
            <MenuItem Header="Menu" Background="Gray">
                <MenuItem Header="Dodaj Pracownika" Command="{Binding RegisterCommand}"/>
                <MenuItem Header="Utórz Harmonogram"/>
                <MenuItem Header="Home" Command="{Binding ShowCommand}"/>
            </MenuItem>
        </Menu>

    </Grid>
</hc:Window>
